# Tasks

Follow-ups generated from findings.md. See Standing rules in CLAUDE.md. Check off, do not delete. Each task names the entry it came from and the role that should do it.

## Open

- [x] T-001 (done 2026-09-18 as F-006, confirmed V-006) (from V-004, experimentalist): fix `shuffle_preserving_degree` in analysis/controls.py. Duplicate-edge check must run against the live edge set, not the original matrix (currently 7296 edges collapse to 6825). Report a multi-seed range instead of one seed-0 draw. End controls.py and leak.py with an assert on preserved in/out degree.
- [x] T-002 (done 2026-09-18 as F-008: masked ULM 0.34 vs masked Huber 0.30, predicted 0.26; confirmed V-008) (from V-003, experimentalist): masked Huber-vs-ULM comparison. Predicted before running: Huber stays below masked ULM's 34%. If Huber reaches 34% or more, README line 16 ("signal lives in the strongly responding targets") needs revising and robust losses reopen.
- [x] T-003 (done 2026-09-18 as F-007, confirmed V-007) (from V-004, experimentalist): random-gene mask control. Zero a random non-target gene per experiment and confirm the 57→28 collapse does not reproduce.
- [x] T-004 (done 2026-09-18 as F-010, confirmed V-010) (from V-004, experimentalist): separate the autoregulatory self-edge path (71% of perturbed TFs have one in CollecTRI) from the upstream-regulon path in the leak.
- [x] T-005 (done 2026-09-18 as F-009, confirmed V-009: gene kept) (from V-005, lit-scout): find Supplementary Table S1 or released code of the 2026 Perturb-seq benchmark and settle whether the perturbed TF's gene stays in the input matrix. Resolves F-005 part 2b.
- [x] T-006 (done 2026-09-18; revisit the shuffle sentence when T-001 lands and the 2026-paper sentence when T-005 lands) (from V-004, V-005, main session): README.md and refs/README.md wording. "survives a degree-preserving shuffle" is pending T-001; "has the same leak" / "never masks that gene" becomes "selects on the TF's own mRNA change and reports no masking step" until T-005 resolves.
- [x] T-007 (done 2026-09-18 as F-013, confirmed V-013) (from V-005, experimentalist): new F entry superseding F-005's "94%" with "up to 94% (weight 2; 77% at weight 1, 66% at 0.5)". F-005 itself is not edited.
- [x] T-008 (done 2026-09-18 in F-013: 279 scored + 90 TF not in CollecTRI + 19 under tmin=5) (from V-001, experimentalist): 109 of 388 knockTF experiments are dropped because the TF has no ULM score. State this wherever the 45% / 34% baselines are quoted.
- [x] T-009 (done 2026-09-18 in F-014, confirmed by V-014: survives unchanged) (from V-002, experimentalist): TF-level (one row per TF) rerun of the overlap and size stratification, since 279 experiments over 155 TFs are not independent.
- [x] T-010 (done 2026-09-18 as F-027: the leak does NOT transfer to their headline metric on their one public dataset, -0.0001 against a 0.05 threshold; the anchor reproduces their shipped numbers to six decimals) (from V-009, experimentalist): masked rerun of the 2026 benchmark's one public dataset (Dox/TP53, in the skeptic's scratch clone; re-clone if gone) through their dcp.py + auc.py with the TP53 column zeroed and the CollecTRI self-edge removed. Report AUROC and rank before vs after. Prediction from V-009: measurable AUROC drop under ULM, because TP53 has a self-edge. Their code needs two fixes to run (`.asType`, `auprc` before assignment). Needs scanpy; check the uv env before starting, a new dependency is a judgment call to log.
- [ ] T-011 (unparked at LOW priority by F-027: on this dataset the channel does not matter for curated-network ULM, but pySCENIC lists the TF as its own target in 198 of 233 regulons, a much wider channel, still untested) (from V-009, lead only): pySCENIC's shipped network lists the TF as its own target in 198 of 233 regulons. Data-driven methods may leak abundance more than curated-network ULM. Park until T-010 says whether the channel matters at all.
- [x] T-012 (done 2026-09-18 as F-012 in a new script coregmask.py, confirmed V-012) (from V-007, experimentalist): randmask.py owes the control V-007 had to run from scratch: an arm masking a gene that shares the perturbed TF's upstream regulators and has a comparable negative logFC (skeptic got 0.441 ULM / 0.563 propagated for top-100 co-regulated, most negative). Print the |logFC| of masked genes, report the regulon gap on the 279 scored experiments (2.69, not 2.05), and assert zeroing for every seed and arm. New F entry, F-007 is not edited.
- [x] T-013 (done 2026-09-18, selfedge.py stdout identical before/after; skeptic checking with F-011) (from V-006, experimentalist, small): delete dead `As = Ashuf[0]` in controls.py; `.all()` instead of `.any()` in leak.py's assert; either preserve signed in-degree in the shuffle or state in controls.py that it is not preserved. Quote the masked result as "inside the null band", never as a margin.
- [x] T-014 (done 2026-09-18 with V-010 wording) (from V-007, main session, after F-010 has a verdict): README framing. For the 71% of TFs with a self-edge the masked value is arguably legitimate regulon signal, not only an artifact. The clean abundance-leak evidence is the no-self-edge subset (n=82): propagation 0.341 -> 0.195 masked while ULM stays 0.244. Also note "up sum" propagation algebraically contains "+k * own logFC" (k = number of upstream regulators, ~21).
- [x] T-015 (answered in V-010 from scratch scripts: unmasked gap is the self-edge, residual gap after self-edge removal is regulon size, 97 vs 23 median targets; firm-up is T-018) (from F-010, experimentalist, after V-010): the self-edge group scores 0.538 vs 0.244 for the no-self-edge group before any masking, and 0.386 vs 0.244 after. Unexplained; candidate causes are regulon size and curation depth. Any claim conditioned on self-edges is confounded until this is split out.
- [x] T-016 (folded into T-027 by F-015) (from F-010, method-designer then experimentalist): "drop the perturbed TF's self-edge at scoring time" as the masked-equivalent baseline for benchmarks where the matrix cannot be edited. F-010 says it reproduces masking for ULM exactly (96/279 both). It is the fix T-010 needs.
- [x] T-017 (done 2026-09-18 as F-011, confirmed V-011) (from V-010, experimentalist): split the 197 self-edge experiments by the SIGN of the self-edge. Every knockdown has own logFC < 0, so positive autoregulators gain from the unmasked benchmark and negative ones are penalised. Prediction to state first: removing the self-edge hurts positive-self-edge TFs and helps negative-self-edge TFs.
- [x] T-018 (done 2026-09-18 as F-014, V-014 split) (from V-010, experimentalist): one-row-per-TF, target-count-matched comparison of self-edge vs no-self-edge TFs after self-edge removal, as a committed script (V-010's estimate has cells of n=36 and n=4, roughly +/-0.07). Fold in T-009 (TF-level rerun of the F-002 stratification), same machinery.
- [x] T-019 (done 2026-09-18 with T-013) (from V-010, experimentalist, small, with T-013): selfedge.py's assert should be `np.array_equal(adjm, saved_copy)` over the whole adjacency, not only the 412 self-edge cells.
- [x] T-020 (closed 2026-09-18 in F-014 as unanswerable in knockTF: -1 TFs are 3/3/4/2 per size bin) (V-011 already did the TF-level sign test: 81/84 and 11/12. What is left is the size-matched TF-level comparison, where the -1 cells are 3 TFs or fewer per size bin and may be unanswerable in knockTF; do it inside T-018 or close it as unanswerable) (from F-011, experimentalist, with T-018): TF-level rerun of the sign split, and check whether the 12 negative-autoregulator TFs are just the deeply curated hubs (median 330 targets), which would make sign and size inseparable in CollecTRI as it stands.
- [x] T-021 (design done 2026-09-18, entered as F-015; V-015 unverifiable, see T-027) (from chat 2026-09-18, method-designer): hierarchical empirical-Bayes slope model for activity-vs-abundance decoupling. Per-TF slope (ULM activity change per unit own-mRNA logFC, self-edge dropped per F-010) with partial pooling across TFs (candidate groupings: self-edge sign label per F-011, noting V-011: +1 includes sign-unknown edges, and dropping the self-edge removes a penalty for 12 TFs as well as a reward for 84; regulon-size tercile per V-010), conjugate normal prior so the posterior is closed form in numpy and reduces to ULM / per-TF OLS with a flat prior. Output: posterior mean and interval for the residual (activity minus abundance-predicted activity). Must state a numeric prediction and how it is judged without rank-among-TFs: interval coverage on held-out knockdowns (residual should cover 0), and SMAD2/3 residual excluding 0 on the TGF-beta set. No PyMC/Stan.
- [x] T-022 (done 2026-09-18, selfsign.py stdout identical) (from V-011, experimentalist, small): selfsign.py's assert should test direction (`es_full < es_ns` for every +1 experiment, `>` for every -1); the current second clause follows from the first. Runtime note is ~30 s, not 2.5 min.
- [x] T-023 (done 2026-09-18, entered as F-016. The lit-scout concluded the split was impossible; it is not, the fetched parquet has a sign_decision column) (from V-011, lit-scout): how many of the 84 "+1" self-edge TFs have a curated activating sign versus CollecTRI's default fill ("TF-gene interactions without any information were assigned an activating mode of regulation", Müller-Dott 2023 Methods)? Needs CollecTRI's per-edge sign evidence, which is not in data/collectri.parquet as fetched by decoupler. Find where it is published (OmniPath, the paper's supplement or Zenodo).
- [x] T-024 (done 2026-09-18: ThreadPoolExecutor, masked_huber.py 482 s vs ~55 min, stdout identical to the skeptic's serial run) (from chat 2026-09-18, main session, after V-008 lands so the file is not edited under the skeptic): robust_ulm.py runs on 1 of 48 threads (25 min). The loop over 388 experiments at line 29 is embarrassingly parallel: stdlib `ProcessPoolExecutor` over experiments, W shared by fork. Expected ~2 min; numbers must be bit-identical (same float64 ops per experiment), check by diffing stdout. No GPU: would need torch or cupy, a new dependency, for a job a process pool already brings under 2 min.
- [x] T-025 (done 2026-09-18, masked_huber.py prints 106/68/76/64 vs 20/20/20/20 and own-gene weight 0.094, matching V-008) (from V-008, experimentalist, small): masked_huber.py should print the 197/82 self-edge split and the final Huber weight on the perturbed TF's own gene (huber_ulm already returns w; the script discards it), so the mechanism is in the repo and not only in V-008. Add to its assert that the masked Huber scores differ from robust_ulm's unmasked S2.
- [ ] T-026 (from V-008, parked): masked run of V-003's corrected Huber (weighted intercept, centred MAD, converged) on all 279. Only needed if anyone wants to cite the masked residual (12 hits, 8 of them large-regulon self-edge TFs) or reopen robust losses.
- [ ] T-027 (BLOCKED on Brandon, research direction. T-034 is done: knockTF, filtered or not, cannot test decoupling; see the README status paragraph. After V-018 the choice is two-way: (a) get a dose-resolved Perturb-seq calibration set, which means large data and a scanpy-class dependency, or (c) stop at the benchmark findings. Option (b), a constant-offset model, is ruled out by V-018 as not an abundance model. EB-slope as designed in F-015 is dead either way.) (from F-015, experimentalist, only after V-015 and with whatever design changes it demands): implement EB-slope as analysis/ebslope.py with the shuffled-network and placebo-regressor controls and the TGF-beta arm. T-016 folds into this: F-015 makes "self-edge dropped" the input, not a separate baseline.
- [x] T-028 (done 2026-09-18 as F-017, V-017 split) (from F-016, experimentalist, started as F-017): split F-011's +1 self-edge group by sign_decision (PMID 74 experiments, default activation 92, regulon 10). How many of the 33 hits ULM loses on self-edge removal rest on a default-filled sign?
- [x] T-032 (done 2026-09-18 as F-020, confirmed V-020) (from V-012, experimentalist, small, batch with T-029): coregmask.py: print the masked-gene table and shared-regulator mean on the 279 scored experiments, skip or label coreg picks when the TF gene has no regulator (53 experiments, 19 scored), logfc floor max(memb-3, 1), and skip already-zero cells so the assert can require picks to be nonzero beforehand.
- [x] T-029 (done 2026-09-18 as F-019, confirmed V-019) (from V-014, experimentalist, small): tflevel.py: tie-averaged matcher instead of np.argmin (ties were broken alphabetically and decided the sign), two-pool re-matching bootstrap, replace the vacuous `lo <= d <= hi` assert clause (e.g. `gap.max() < np.log10(2)`), print F-002's TF-level table at the experiment-level cut points, state the n=2 median convention. New F entry superseding F-014 part 3 with the weakened wording. V-014's judgement: 59 no-self-edge TFs with 11 hits probably cannot settle the size question in knockTF at all.
- [x] T-030 (done 2026-09-18 as F-018: all six preview numbers reproduced, within-TF slope CI includes 0, per-TF mean beats per-TF slope out of sample; V-018 split: the predicted slope is rejected with 73% power; a slope near 0.5 is undecidable; direction untested, not refuted) (from V-015, experimentalist, ~1 h, no direction decision needed): commit V-015's preview as analysis/slopecheck.py so the premise test is a real finding, not a scratch preview. Self-edge-dropped ULM score a vs own logFC m on the 279: corr(a, m), slope with intercept, within-TF slope (TF intercepts, 176 experiments of the 52 multi-experiment TFs), own-logFC permuted across experiments within size tercile (500 permutations) vs real, and LOO RMSE on the 176 for: null, global slope, global mean, per-TF slope, per-TF mean; all with and without REST (5 experiments, 42% of the null's squared error) and with a TF-cluster bootstrap. Predictions from V-015's preview: corr 0.10, within-TF slope 0.08 +/- 0.34, placebo mu 0.75 vs real 0.82, per-TF mean beats per-TF slope (2.74 vs 3.19).
- [ ] T-031 (NOT answered by F-021; V-021 scratch preview: all defaults -> -1 gives 126 -> 61 unmasked and 96 -> 62 masked, i.e. about half of default-activating's win is the leak and half survives masking. To be committed in T-036. Still open after that: the paper's own metric, the 1895 regulon-tier edges, zeroing vs flipping) (from V-016, lead, parked): the CollecTRI paper chose default-activating because it beat default-repressing on an unmasked knockTF-style benchmark (t = 337). By F-010/F-011 a default +1 on 343 self-edges is itself rewarded by that benchmark. How much of that win is the self-edge leak? Testable here: rescore knockTF with default-fill edges flipped or zeroed, masked vs unmasked.
- [x] T-033 (done 2026-09-18 as F-021, V-021 split) (from V-017, experimentalist, started as F-021): commit the skeptic's sign-intervention probe and the repressor-majority lead as analysis/signprobe.py with predictions stated first. Also the two small signsource.py fixes V-017 asks for (assert 19396 / 1895 / 21699; drop the false closing NOTE).
- [x] T-034 (done 2026-09-18 as F-022: within-TF slope 0.48, CI excludes 0, but fails leverage and pays nothing out of sample; masked ULM 0.30 where own logFC >= 0; V-022 split: dose-response exists, size uncertain, does not pay out of sample; the "not only abundance" headline refuted, exact zeros are unmeasured genes) (from V-018, experimentalist, started as F-022): rerun the slope check on the UNFILTERED knockTF (data/knocktf_full.h5ad, 907 experiments, 456 TFs; 379 with own logFC in (-1, 0), 140 at or above 0; 76 TFs with within-TF range above 1 logFC, 32 above 2). This is the dose-resolved set V-018 said was required, with no new dependency. Also the benchmark by dose bin: if ULM recovers the TF only when its own mRNA dropped, that is the abundance-vs-activity question asked directly.
- [ ] T-035 (from V-019, experimentalist, trivial, batch with the next tflevel.py edit): print the lower-median overlap and size contrasts next to block (7) and caveat the "+0.090" line as a median-convention artefact; drop the can't-fail "tie weights sum to 1" assert clause.
- [ ] T-036 (from V-021, experimentalist; V-022 has landed: for the replication on knocktf_full use only experiments with own logFC < 0 and nonzero, since exact 0.0 means not measured, and compare against the per-TF base rate rather than 10%): extend analysis/signprobe.py with predictions stated first: 50 seeds for the two random controls; within-TF sign permutation arm (V-021 scratch: mean 82.7, 0 of 50 reach 96); least-supported-k removal arm (mean 89.7); all defaults -> -1, masked and unmasked, plus the self-edge variants (V-021 table); Part B effect on the <=150-target bins, and its replication with the class definition FIXED NOW (PMID -1 count > PMID +1 count, TF has default edges) on the experiments in data/knocktf_full.h5ad that are not in data/knocktf.h5ad, one vote per TF. Docstring fixes (runtime 45 s; the self-contradictory 5a sentence) and signsource.py block (4) header. New F entry superseding F-021's first sentence and its "below every seed".
- [x] T-037 (done 2026-09-18 as F-026, V-026 split) (from V-022, experimentalist): same-study lead against the 34% baseline. In studies that perturbed more than one TF, masked ULM puts the perturbed TF top-10% in 0.277 of experiments and the same TF in 0.225 of its study-mates' experiments (155 experiments, 45 studies; difference +0.052, CI includes 0), against 0.131 in other studies. Study-mates share regulon targets (Jaccard 0.066 vs 0.024). Commit it as a script with the prediction stated first, and redo it excluding study-mates with regulon Jaccard above 0.05. If it holds, ULM is detecting the study's biology more than the perturbed TF.
- [ ] T-038 (from V-022, main session, small): every future script should compare hit rates with the per-TF base rate (about 0.12), not 0.10; V-022's size-matched null is exact for univariate ULM and needs no seeds (read the score of the 20 nearest-size other TFs in the same masked experiment).
- [x] T-039 (done 2026-09-18 as V-023: reading 1 refuted, reading 2 unverifiable, reading 3 confirmed. Follow-ups are T-040, T-041, T-042) (from the option-A scoping, skeptic first then experimentalist): Sugimoto 2026 (refs/sugimoto-2026-tfactprofiler.md) benchmarks TF activity against phosphoproteomics, which is ground truth for activity abundance cannot explain, and reports CollecTRI+ULM losing to their resource. Verify the quotes and the design, then decide whether the MPXV transcriptome+phosphoproteome set (public, linked in their methods) is a cheaper validation route than any Perturb-seq download.
- [x] T-040 (done 2026-09-18 as F-024: INCONCLUSIVE at n=51. Masked ULM vs protein-corrected phospho activity Spearman -0.018 [-0.33,+0.33]; shuffled null +0.097 spanning [-0.07,+0.29]; placebo +0.029, p=0.590; uncorrected phospho gives +0.209 [-0.16,+0.54], so the abundance correction is what removes the apparent signal. Needs ~206 TFs for +/-0.10. Follow-ups T-043, T-044, T-045) (from V-023, experimentalist, started as F-024): build the phospho premise check ourselves rather than citing Sugimoto's. Pooled over TF-site-timepoint observations, our own SIGNOR filter (EFFECT up-/down-regulates ACTIVITY only, never QUANTITY), protein-abundance-corrected with Dataset 4's protein_fold_change_log2 columns, masked-gene ULM on CollecTRI, against a shuffled-network null.
- [ ] T-041 (from V-023, main session, small but repo-wide): refs/jats2md.py silently drops sub-<sec> bodies. Every numbered Methods sub-section of Sugimoto 2026 is missing from refs/ and present in the Europe PMC XML. Other refs/ files may be truncated the same way; check muller-dott and tf-activity-benchmark first, since verdicts cite their Methods.
- [ ] T-042 (from F-023, parked): test the implied prediction on Sugimoto's released resource (Zenodo 20779282) that masking the perturbed TF's own gene costs TFActProfiler more than CollecTRI. Only worth it if the project ever cites their comparison.
- [ ] T-043 (from F-024, parked until a bigger panel exists): the F-024 design is correct and the data is too small -- 19 TFs, 51 observations, CI half-width 0.33, and 6 of 10 degree-preserving shuffled networks beat the real one. Do NOT rerun it on another single phosphoproteome. It becomes decidable at about 206 TFs with a directional quantified SIGNOR site, i.e. roughly ten MPXV-sized runs pooled, or one deep run with ~130 directional TF sites at three timepoints. The reusable part is analysis/phospho.py: the SIGNOR activity-only filter, the stdlib xlsx reader, the protein-corrected activity, and both nulls all take a new (transcriptome log2FC, phosphosite log2FC + protein log2FC) pair as input.
- [ ] T-044 (from F-024, experimentalist, cheap and the most interesting number in it): uncorrected phospho activity correlates with ULM at Spearman +0.21 and protein-corrected at -0.02, on nearly the same observations. If that survives -- restricted to the 51 observations where both exist, with a paired TF-cluster bootstrap of the DIFFERENCE rather than two overlapping CIs -- it is a direct measurement of how much of a phosphosite benchmark is a protein-abundance readout, which is F-005/F-009/F-023's claim in a setting with no perturbed TF. Same script pattern, one new script.
- [ ] T-045 (from F-024, main session, wording): V-023 reported 2464 SIGNOR sites on 1079 proteins and 23 panel TFs; F-024's rerun of the same URL and filter gives 2335 / 1043 and 19 TFs (50 conflicting up/down sites dropped, residue-string parsing, and CollecTRI pruned at tmin=5 against the 12971 genes of THIS transcriptome rather than all CollecTRI targets). Neither is wrong; cite the F-024 numbers when the panel is the unit and say which pruning produced them.
- [ ] T-046 (from V-024, experimentalist, the cheap high-value one): replicate the two results V-024 promoted, on a SECOND public phosphoproteome+transcriptome pair. (i) the paired correction drop, d = -0.245, one-sided p 0.034; (ii) the mechanism, masked ULM vs the protein-abundance component +0.360 [-0.03, +0.65] vs the occupancy component -0.018. V-024's own flip condition for (ii): it dies if it turns out to be the panel's protein levels tracking the global transcriptome response rather than anything TF-specific, so ship a per-timepoint-centred arm and a protein-level null. Pooling resolves effects of 0.2-0.3, which is exactly this size, so this is worth doing and the +/-0.10 question is not.
- [ ] T-047 (from V-024, lit-scout then experimentalist): the TF-sparing ground truth is the binding constraint, not the number of datasets. Only 174 of CollecTRI's 1185 sources have a directional SIGNOR activity phosphosite at all (142 scoreable at tmin=5 here). Scope PhosphoSitePlus regulatory sites and kinase-substrate activity models as a broader ground truth, and report how many CollecTRI TFs each would cover. This decides whether the phospho route has a ceiling worth living with.
- [ ] T-048 (from V-024, parked until T-047): the other TF-sparing routes, which do not share SIGNOR's ceiling — upstream-regulator perturbations (KEAP1 -> NFE2L2, VHL -> HIF1A, MDM2 -> TP53) and signalling stimuli. These need the Perturb-seq decision or a stimulus dataset, so they stay parked.
- [ ] T-049 (from V-025, the experiment that would actually decide it): the two readings of F-024's protein correlation cannot be separated by more TFs, only WITHIN a TF. Needs protein level and regulon response measured across several levels of the SAME TF on a TF-sparing route (a dose-resolved stimulus, an inducible system, or a degron). If the between-TF ranking (+0.36) has no within-TF counterpart anywhere, it is a common-driver artefact and the reading "ULM detects abundance-driven activity through its regulon" dies. V-025's own within-TF number here is -0.118, positive in 7 of 17, on 3 timepoints each.
- [ ] T-050 (from V-025, experimentalist, fold into T-046): the protein correlation's sign construction does half the work. Report the UNSIGNED number (+0.204 pooled, +0.331 on the 12 positive-sign TFs, +0.028 on the 7 negative-sign) next to the signed one wherever it is cited, and prefer a panel with a balanced sign mix in any replication.
- [ ] T-051 (from F-025, skeptic): verify the novelty claim. Both precedents are now in refs/ in full (trescher-2019, yashar-2024). Check the quotes, check whether either actually performs an ablation that removes the perturbed gene, and check whether Priori's own-expression variant is the same comparison as F-005's "own logFC alone" or a different statistic. The negative half matters most: is "not reported" safe to write, given the scout searched one 176-paper corpus?
- [ ] T-052 (from F-025, lit-scout or main session): read refs/epiregulon-2025.md. It builds GRNs from paired scATAC+scRNA specifically because expression-only methods miss post-transcriptional TF modulation, i.e. it targets the same gap this project has been circling. Does it validate on knockTF, does it mask anything, and does it make the decoupling direction redundant?
- [x] T-053 (done 2026-09-18 as F-028, verdict pending) (from V-026, experimentalist, started as F-028): commit the study-context lift as its own claim. On the 106 scored experiments that have BOTH a study-mate and a same-cell-line different-study mate: own 0.245, same-cell other-study mate 0.163 (d +0.082 [+0.027, +0.188]), within-study mate 0.236 (d +0.009 [-0.072, +0.071]); the paired gap between the two mate sets is +0.073, study-CI [+0.001, +0.192], TF-CI [+0.018, +0.128]. This is the one comparison here whose CI excludes 0 in a design where both arms share the judged row.
- [x] T-054 (done 2026-09-18 in F-028: symmetric mask, core d +0.056 -> +0.066, baseline 0.344 -> 0.348) (from V-026, experimentalist, small, fold into T-053): make the symmetric mask the default in studymate.py (all perturbed-TF genes zeroed in every row; one extra ULM run, d +0.056 -> +0.066). Note when citing F-026 that its Confidence field says GSE31912 supplies 78 of the 155; it is 38.
- [ ] T-055 (from V-026, note for any future within-study contrast on knockTF): MDE is +0.11 at 80% power and about 755 eligible experiments would be needed for +0.05, against the 155 knockTF supplies. Retire V-022's Jaccard flip condition rather than rerunning it.
- [x] T-056 (done 2026-09-18 in V-028: the 20-seed sweep found one more offender, the symmetric core d [+0.003, +0.134], positive in 2 of 20. V-019 self-flags; V-024 is load-bearing at its upper end. Index and README corrected.) (from F-028, main session, repo-wide hygiene): F-028 found V-026's asymmetric lift lower bound is -0.0011 on a rerun, not the +0.001 V-026's scratch reported, so that CI-excludes-0 claim survives only on the symmetric row. Sweep findings.md for any other bootstrap bound quoted within 0.002 of zero as if decisive; the skeptic is doing this as part of V-028, so fold in whatever it finds.
- [ ] T-057 (from F-028, decide after V-028): does the symmetric mask become the convention in leak.py and fulldose.py too, or stay local to studymate.py? It changes the filtered-279 baseline from 0.344 to 0.348, which is inside noise but would touch every number in the log.
- [ ] T-058 (from V-028, experimentalist, small): studymate.py should print the PAIR-weighted lift (-0.001) beside the row-weighted one (+0.073) and the per-study table (GSE31912 -0.004 vs the other 23 studies +0.117), since judgment call (d) is doing all the work. Also port the placebo-TF null in as a standing control: cheaper than the 10-seed shuffle and strictly stronger.
- [ ] T-059 (from V-028, the open scientific question): pathway co-selection vs shared reference sample. The data cannot separate why the series-wide component aligns with the perturbed TF's regulon. Needs a resource exposing control-sample IDs (knockTF2 web metadata) or ~800 eligible rows. The GSE31912 pattern favours co-selection.
- [ ] T-060 (from F-027, RESTATED by V-027 with 100 seeds): on their one public dataset a shape-matched random regulon scores 0.47 +/- 0.14, central 95% [0.225, 0.762]. That band covers 34 of their 60 published columns for TP53__Dox, including their own `random` column; metaTF_PUIC 0.734 is inside it. If the band holds on their other datasets, several of their published rankings are not separable from a random network of the right shape. Needs their other 16 datasets, or the same shuffle applied to any public Perturb-seq benchmark.
- [ ] T-061 (from F-027, two defects to report to the authors, not to fix here): (i) result/auc/rank/*.csv contains RANDOM ranks for every method, because auc.py line 222 dumps df_rank after the line-199 `if(True)` block rebinds it; the headline au_rank is computed earlier and is correct. (ii) ORA on sparse single-cell counts is unstable to deleting any single column (decoupler _ora.py ordinal tie-breaking at the top-775 cut), which affects their ORA_CollecTRI / ORA_TFLink / ORA_ChIP-Atlas figures.
- [ ] T-062 (from F-027): the leak's size is regulon-size- and knockdown-strength-dependent, so T-010's answer is "not in this regime". The decisive test is a public Perturb-seq knockdown of a TF with under 60 CollecTRI targets and a detectable mRNA drop, which is where V-010 says the self-edge is worth half the hits.
- [ ] T-063 (from V-027, new entry candidate, experimentalist): decoupler 2.2.0's ORA is unusable on sparse single-cell counts. Rescoring an UNCHANGED matrix under different feature orderings moves ORA AUROC by 0.169-0.189, because decoupler permutes all features with a seed that depends on the feature count and masking drops the now-all-zero gene. Separately, `_ora.py` selects the complement of the bottom 5%, i.e. 95% of features, not the top 5% its docstring claims, and it raises `ValueError: invalid contingency table` on the full 21985-gene knockTF matrix with default n_bg=20000. On bulk logFC the cut lands in a tie block in 12% of rows with median tie size 1 and three orderings give identical hit rates, so this is specific to sparse counts. Worth its own F entry and a note to decoupler.
- [ ] T-064 (from V-027): sweep the log for any other 10-seed null quoted as a range or a mean; V-024 and V-027 are the two found so far, and CLAUDE.md now requires 100 draws.
- [ ] T-065 (from F-029, small): generate the two missing persisted files — `results/masked_huber.*` (8 min) and `results/bench2026.*` (43 min). The code is written in both scripts and has never executed, so it is unexercised.
- [ ] T-066 (from F-029): master.csv holds point estimates and covariates, not draws, so no bootstrap CI, shuffled null or within-TF slope in the log is recomputable from it. Decide whether the owning scripts should also persist their draws, or whether rerunning is the right recovery path.
- [ ] T-067 (from V-029): add a raw ULM score column, or a companion scores file, to master.csv. Without it F-022's within-TF slope and bin means, V-025's +0.360 and V-027's size law are not recomputable from the record even as point estimates — the file holds ranks and flags only.
- [ ] T-068 (from V-029, closed-unless-wanted): V-022's scratch is deleted and reproduces neither candidate construction exactly (masked 7 of 10 printed bins, unmasked 6 of 10). Which of its two rows carries the 0.001 error is unrecoverable. Do not spend further time on it; publish both numbers with their denominators.

## Done

## Decisions

- 2026-09-18: tasks and decisions live in this one file rather than in findings.md. Reason: findings.md is append-only claims and verdicts with a fixed format; task state needs editing.
- 2026-09-18: started T-001+T-003 (one experimentalist, shared files), T-002 (second experimentalist) and T-005 (lit-scout) in parallel right after the verification pass. Reason: T-001 is a bug in a mandatory control, T-002 and T-005 are the two caveats that limit how F-003 and F-005 can be cited; F-numbers F-006/F-007/F-008 pre-assigned to avoid collisions in the append-only log.
- 2026-09-18: README.md quotes the skeptic's corrected-shuffle number (~31%) before T-001 lands. Reason: the old sentence was known wrong; the new one is flagged as pending.
- 2026-09-18: sharpened T-004 after V-009 and started it as F-010. Reason: V-009 points out ULM is univariate, so the TF's own mRNA reaches its own score only through a self-edge; that makes the 45->34 masking drop a testable prediction (it should sit almost entirely in the 71% of TFs with a self-edge) instead of a vague "separate the paths".
- 2026-09-18: of the method families Brandon raised in chat (diffusion, graph attention, optimal transport, neuro-symbolic, Bayesian, VAE) only Bayesian partial pooling was filed (T-021). Reason: the bottleneck is identifiability and the benchmark, not capacity; more expressive models find the own-mRNA leak faster. Pooling addresses a real constraint (under 2 experiments per TF) and stays a weighted mean. Symbolic PTM-regulation priors as a validation target for the residual channel is a possible later task, not filed.
- 2026-09-18: T-024 uses threads, not the process pool first planned. Reason: a process pool started at import time deadlocks (the parent pickles the worker function, which needs the import lock of the module still being imported); numpy releases the GIL here, and a synthetic benchmark gave 7.0x for both threads and processes with bit-identical output. Memory bandwidth caps it at ~7x on 24 cores. No findings entry: no number changed.
- 2026-09-18: with findings.md fully verdicted, started the remaining no-input tasks in four parallel agents: small script follow-ups T-012/T-007/T-008/T-022/T-025 (F-012, F-013), TF-level rerun T-018/T-009/T-020 (F-014), Bayesian slope design T-021 (method-designer, will be entered as F-015), CollecTRI sign evidence T-023 (lit-scout). Held back: T-010 (2026-paper rerun needs scanpy, a dependency decision, and their code patched) and T-016 (wait for T-021's design, which decides whether "drop the self-edge" is the baseline). T-026 stays parked.
- 2026-09-18: overrode the lit-scout's T-023 conclusion ("not possible from any public file") after opening data/collectri.parquet: it has a sign_decision column. Reason: the scout cannot run code and V-011 had asserted the column was absent; one read of the file settles it. Entered as F-016 with the counts, for the skeptic to check like any other claim.
- 2026-09-18: STOPPED T-027 and flagged the README direction instead of redesigning it myself. Reason: V-015 undermines "a slope calibrated on knockdown data" as identifiable in knockTF; choosing between (a) a dose-resolved Perturb-seq calibration set (new data, scanpy dependency), (b) a constant-offset model, (c) dropping decoupling is a change of research direction, which the standing rules reserve for Brandon. T-030 (commit the premise check) goes ahead because every option needs it.
- 2026-09-18: moved stray T.csv and prev.csv from the repo root to the session scratchpad. Reason: V-015's skeptic listed them as its scratch outputs; its scripts wrote them with a relative path. Not deleted, they back the V-015 preview numbers.
- 2026-09-18: fetched the unfiltered knockTF into data/knocktf_full.h5ad and started T-034 without waiting for the direction decision. Reason: V-018 said the direction is untested because decoupler's copy is truncated at own logFC < -1; reading decoupler/ds/_bulk.py shows that is the default `thr_fc=-1` of `dc.ds.knocktf`, so the dose-resolved data is one argument away, same source, no dependency. It tests the existing direction rather than changing it, and the existing data/knocktf.h5ad is untouched.
- 2026-09-18: my README sentence calling the 140 experiments with own logFC >= 0 "a natural negative control" was wrong and is withdrawn (V-022: 82 of those 140 are unmeasured genes, most of the rest are inside noise). Four of my six blind predictions for fulldose.py failed; the two that held were the slope and the shuffle null.
- 2026-09-18: Brandon added a second Ariadne citation graph, seeded on Replogle 2022 / Perturb-seq; moved to refs/ariadne-corpus-perturbseq.md and noted in refs/README.md. Started a lit-scout scoping option (a) (dose-resolved calibration set) against it. No dataset is downloaded and no dependency added; this is a go/no-go scoping pass only.
- 2026-09-18: correction to my own chat answer: I described Replogle 2022 "from memory, not from refs/" when refs/replogle-2022-genome-scale-perturbseq.md has been present since 2026-09-18 07:59 with full STAR Methods. Check refs/ before caveating an answer as unsourced.
- 2026-09-18: my chat summary of Sugimoto 2026 overstated it. I reported "TF mRNA carries no information about post-translational activity" and "CollecTRI+ULM tracks phospho-activity significantly worse"; V-023 refuted the first as an underpowered null and found the second unreportable (no test, no n, no effect size). Corrected to Brandon in the next message. Lesson: a quoted number with a CI is not a result until the n and the unit of observation are known, and neither was in the paper.
- 2026-09-18: started T-040 (the phospho check, ~27 MB of public xlsx/TSV, no new dependency) without a direction decision. Reason: it tests the premise of the EXISTING direction and needs no masked-gene control by construction (there is no perturbed TF), so it is not a change of direction; the Perturb-seq question stays open for Brandon.
- 2026-09-18: two more errors of mine reported to Brandon in chat and corrected here. (i) I repeated F-024's "6 of 10 shuffled networks beat the real one"; V-024 shows that is a retrieval-column count attached to the Spearman sentence, and the right numbers are 9 of 10 and 33 of 50. (ii) I told Brandon that pooling about ten phosphoproteomes "is what reaches the needed size"; V-024 refutes it — 206 TFs is above the 174-TF ceiling of the whole CollecTRI x SIGNOR-directional resource. Lesson: a projected sample size needs its population checked before it is quoted as a plan.
- 2026-09-18: I challenged V-024's "abundance leak" framing of the +0.360 and V-025 refuted it in my favour on mechanism, but my replacement reading is only one of two the data cannot separate: the effect is entirely between-TF and the sign convention does half the work. Both "ULM is essentially tracking abundance" and "the PTM channel is what ULM misses" are withdrawn. Lesson: when a verdict files a number under an existing family, check the number was computed under the conditions that family names before relaying it.
- 2026-09-18: the novelty question came back "prefigured twice, mechanism not found" (F-025), which reframes what a write-up would claim: the contribution is the self-edge mechanism plus the mask/shuffle/base-rate controls, not the bare observation that own expression predicts the perturbed TF. Fetched both precedents plus KnockTF 2.0, a GRN-benchmarking review, Epiregulon and scTF-seq into refs/.
- 2026-09-18: Brandon authorised adding scanpy for T-010 ("scanpy can fit in this repo's .venv no problem... you have my permission"). Logged per the dependency rule before the add. Reason: the 2026 paper's own pipeline (dcp.py) reads their filtered.h5ad with scanpy and calls sc.pp.normalize_total / log1p; reimplementing their preprocessing would weaken the point, which is that THEIR code on THEIR data moves when one gene is masked. Only `uv add scanpy`; nothing else in the lock file is to change.
- 2026-09-18: three things I told Brandon about F-026 were wrong and are corrected in the next message. (i) "one screen supplying 78 of 155" — it is 38, and removing it changes d by 0.000. (ii) "the well-powered same-cell different-study contrast supports TF-specificity" — V-026 shows matching on cell line LOWERS d (0.141 -> 0.116), so that row is the ordinary base-rate contrast, not a within-study specificity estimate. (iii) "a skeptic's own Jaccard number did not reproduce" — it reproduces exactly as the per-experiment mean over scored mates; the two numbers are different statistics and F-026's inference from its own was the one that was wrong.
- 2026-09-18: my prediction for the shuffled study-context lift (+0.03 to +0.05, "most of it survives, because series structure lives in the expression matrix not the network") was wrong: F-028 measured +0.004, so 6% survives. The lift is regulon-specific, not generic batch variance, which means it is NOT removable by ordinary batch correction and is a more interesting result than the one I predicted.
- 2026-09-18: two more relay errors of mine, corrected to Brandon. (i) I told him the study-context lift is "not removable by ordinary batch correction"; that was F-028's non-sequitur ("regulon-specific therefore not batch") repeated without testing, and V-028 shows series-mean centering takes it from +0.073 to +0.020. (ii) I quoted the symmetric core d +0.066 [+0.003, +0.134] as clearing zero; it is positive in 2 of 20 bootstrap seeds. Pattern across today: I relay a subagent's framing faster than I check whether its number was computed under the conditions the framing assumes.
- 2026-09-18: T-010 came back NEGATIVE for our hypothesis and that is the result. The abundance leak does not transfer to the 2026 benchmark's headline metric on its one public dataset (-0.0001 against a pre-stated 0.05 threshold), because TP53 there has 772 measured targets and its own transcript is detected in 95 of 617 cells and does not drop — the opposite regime from knockTF. V-027 corrects this: F-027 closes only the DIRECT own-mRNA channel, while F-023's mechanism is indirect (weights fitted to TF-transcript covariation), so F-023 is neither weakened nor confirmed. What is regime-dependent is F-009/F-010's direct channel. The rerun's controls found two other problems with the paper instead (T-060, T-061), which is the controls doing their job.
- 2026-09-18: four more corrections to what I relayed. (i) I called the six-decimal anchor "extraordinary"; V-027 says it is expected — ULM is closed-form, decoupler's tie-break is seeded, the h5ad is shipped — and licenses only "version-stable and deterministic". (ii) I repeated "it does not even go DOWN in counts"; on the normalised layer their own filter.py uses it does, and that filter keeps a TF only if it drops, so their benchmark selects on the own-abundance change like knockTF's. (iii) The shuffled band was a 10-seed min-max; 100 seeds give 0.47 +/- 0.14 and 34 of their 60 published columns fall inside, not six. (iv) The ORA mechanism I relayed is wrong twice. Added a CLAUDE.md convention: ten seeds is not a null.
- 2026-09-19: Brandon asked where the actual result files were — only gapcheck.csv existed. Building results/master.csv to answer that immediately found a wrong number in V-022 that had propagated to the Index and to my write-up (base rate on the 279 is 0.122, not 0.123). studymate.py had been printing the correction for hours. The convention that said "print the table" and not "keep it" is what hid it.
- 2026-09-19: V-029 confirmed the base-rate correction (0.122 on the 279) but REFUTED my explanation of it, which I had already published in three places. I wrote that V-022 "quoted the wider figure against the narrower denominator"; an unmasked-rank construction fits V-022's 279 row and misses its 643 instead, the mirror image, and its scratch is gone so neither is recoverable. Lesson: a correction is two claims — the number and the story — and I shipped the story at the number's confidence.
- 2026-09-18: V-003 left after the V-004 erratum in findings.md. Reason: append-only beats numeric order.
- 2026-09-18: in F-024 I made four judgment calls rather than asking. (1) Phospho activity is the MEAN over a TF's directional sites, not the signed sum, so YAP1 (7 sites) does not outweigh the 12 single-site TFs; the sum version is one line away in analysis/phospho.py. (2) The 50 sites carrying BOTH an up- and a down-regulates-activity annotation are dropped, not majority-voted: their sign is what the whole readout rests on. (3) CollecTRI is pruned at tmin=5 against the genes measured in this transcriptome (622 TFs), which is what the ranking is actually over, giving a 19-TF panel instead of V-023's 23. (4) top-10%/top-5% are undefined with no perturbed TF, so I reused leak.py's evaluate() with sign(phospho activity) in the role of type_p; chance stays 0.10/0.05 by construction. No dependency was added: openpyxl would have saved about 25 lines of stdlib xlsx parsing and is not worth a lock-file change for two files read once.
- 2026-09-18: added `data/*.xlsx` and `data/*.tsv` to .gitignore. Reason: F-024 downloads 27 MB of public supplementary data into data/, and only *.h5ad and *.parquet were ignored, so data/ would otherwise be committed. CLAUDE.md already says data/ is gitignored; this makes that true for the new file types.
